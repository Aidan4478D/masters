\section{Experimentation and Results}

Vehicle detection experiments in this study were centered on a 6-class taxonomy 
designed to support emissions-oriented analysis. Several preliminary configurations 
were explored during early development, but those runs were primarily used to 
understand the difficulty of the task and refine the problem formulation. Thus,
the results presented in this section focus on experiments conducted under the 
final 6-class setting. 

First, YOLO-based experiments are presented, including comparisons across model
variants and refinements through data-centric strategies such as hard negative 
mining and dataset refinement. Other detectors like DETR-based models and Faster
R-CNN were examined to compare architectures within the project’s context. 

After identifying the strongest detector, additional experiments evaluate its 
robustness through threshold and NMS sensitivity analysis, cross-camera generalization,
and agreement between predicted vehicle counts and reference ATR or manual counts. 
Together, these experiments exhibit the practical suitability of the final system 
for traffic-monitoring applications.

% ==============================================================================

\subsection{YOLO-Based Detector Development and Results}

\subsubsection{Baseline YOLO Experiments}

The first YOLO experiments under the MOVES-based taxonomy were conducted by 
fine-tuning baseline YOLO models on the training split and validating them on 
the validation split defined in Section \ref{sec:data-splits}. Each model was 
trained to predict the six final vehicle classes --- passenger car, passenger truck, 
light commercial truck, bus, truck, and motorcycle --- directly from traffic-camera 
images. The purpose of this stage was to assess whether YOLO could learn the 
6-class formulation effectively and provide a strong starting point for later 
refinement.

To evaluate CNN-based one-stage detectors in a controlled and comparable way, 
this study focused on YOLOv8, YOLO11, and YOLO26, which represent three distinct 
generations of the Ultralytics YOLO family. YOLOv8 (2023) was selected as a strong and 
widely adopted baseline, since it introduced the anchor-free split head and was 
designed as a general-purpose real-time detector with a favorable speed-accuracy 
tradeoff \cite{yolov8_ultralytics} \cite{ultralytics_yolov8_vs_yolov11}. 
YOLO11 (2024) was included because it is a newer Ultralytics detector that builds 
on earlier YOLO models with improved feature extraction, training efficiency, and 
parameter efficiency, making it a natural candidate for higher accuracy under 
the same general framework \cite{yolo11_ultralytics} \cite{ultralytics_yolov8_vs_yolov11}. 
As of April 2026, YOLO26 is Ultralytics' most recent YOLO model, tested as an 
edge-oriented variant designed for simpler deployment, with end-to-end NMS-free 
inference and targeted improvements for efficiency and small-object recognition 
\cite{yolo26_ultralytics} \cite{ultralytics_yolov8_vs_yolov11}. 
Evaluating these three models therefore allowed comparison across a 
consistent detector family while covering a useful range of design priorities: 
a mature baseline (YOLOv8), a newer accuracy-focused refinement (YOLO11), and a
deployment-oriented architecture with simplified inference (YOLO26). This made 
them well suited for the present task, which required not only accurate vehicle 
detection but also practical applicability to traffic-monitoring and counting 
workflows. To enable a more controlled comparison across YOLO generations, all 
experiments in this subsection used the small (S) variant, which provided a 
consistent mid-scale tradeoff between model capacity, compute, and efficiency 
\cite{ultralytics_coco}.

To establish baseline performance, each YOLO variant was fine-tuned under the 
same general training configuration. All baseline models were trained for 50 
epochs with a batch size of 8, since the validation mAP curves indicated that 
performance had largely plateaued by that point. Unless otherwise noted, the 
same default augmentation pipeline was used across all YOLO variants so that 
observed differences would primarily reflect architectural differences rather 
than changes in training conditions. During training and evaluation, images 
were resized to $768 \times 1280$, and bounding-box annotations were converted from 
the original CVAT XML format into normalized YOLO label files under the final 
6-class mapping. Instances labeled \verb|unknown_vehicle| were not treated as 
a supervised detector class and were instead handled according to the ignore-region
procedure described in Section \ref{sec:unknown-vehicles}.

\begin{figure}[!htbp]
    \centering
    \begin{subfigure}[c]{0.48\textwidth}
        \centering
        \includegraphics[height=4.5cm]{figures/results/5_1_1_baseline_overlayed_ap50.png}
        \caption{Validation AP50 versus training epoch for the baseline YOLOv8s, YOLOv11s, and YOLOv26s models.}
        \label{fig:baseline-ap50s}
    \end{subfigure}
    \hfill
    \begin{subfigure}[c]{0.48\textwidth}
        \centering
        \includegraphics[height=4.5cm]{figures/results/5_1_1_baseline_overlayed_map.png}
        \caption{Validation mAP versus training epoch for the baseline YOLOv8s, YOLOv11s, and YOLOv26s models.}
        \label{fig:baseline-maps}
    \end{subfigure}
    \caption{Validation AP50 and mAP over 50 epochs for the baseline YOLOv8s, 
             YOLOv11s, and YOLOv26s models. All three models showed rapid early 
             improvement followed by a gradual plateau, with YOLOv26s achieving the 
             strongest baseline mAP and YOLOv11s remaining competitive in AP50.}
    \label{fig:baseline-yolo-charts}
\end{figure}

\FloatBarrier

\begin{table}[!htbp]
    \centering
    \caption{Baseline validation performance of the three YOLO model variants under 
             the final 6-class formulationm reported at the checkpoint with the best 
             validation mAP for each model}
    \label{tab:yolo-baseline-results}
    \begin{tabular}{l c c c}
        \toprule
        Model Checkpoint & Epochs & AP50 & mAP \\
        \midrule
        YOLOv8s  & 50 & 0.750 & 0.606 \\
        YOLOv11s & 50 & 0.804 & 0.615 \\
        YOLOv26s & 50 & 0.802 & 0.632 \\
        \bottomrule
    \end{tabular}
\end{table}

As shown in Table~\ref{tab:yolo-baseline-results} and Figure~\ref{fig:baseline-yolo-charts}, 
all three baseline YOLO variants converged within 50 epochs on the validation set 
under the MOVES-based taxonomy. YOLOv8s achieved an AP50 of 0.750 and a mAP of 0.606,
while YOLOv11s improved this to an AP50 of 0.804 and a mAP of 0.615. YOLOv26s 
produced the highest baseline mAP at 0.632, while remaining close to YOLOv11s 
in AP50 with a value of 0.802. Since YOLOv11s and YOLOv26s represented the strongest 
baseline models in terms of AP50 and mAP, respectively, both were carried forward 
into the subsequent refinement experiments. The next stage of experiments investigated
whether these baseline results could be further improved through data-centric 
refinement strategies.

% ==============================================================================

\subsubsection{Data-Centric Refinements}
\label{sec:data-refinement}

\textbf{Dataset Refinement}

After establishing the baseline YOLO results, the next stage of experimentation 
focused on data-centric refinement guided by manual inspection of model errors. 
Rather than changing the detector architecture, this stage used the baseline 
confusion patterns to identify which types of examples required closer review. 
Particular attention was given to the dominant confusion between passenger car 
and passenger truck, as well as to vehicles that appeared visually ambiguous in 
the original imagery. Model predictions that did not match the validation set 
label were not used to relabel the dataset automatically, but instead served to 
flag candidate examples for manual review. The validation confusion matrices 
before dataset refinement for the baseline YOLOv11s and YOLOv26s models are 
shown in Figure~\ref{fig:val-cm-pre}. These predictions followed the same ignore-aware
evaluation outlined in Section~\ref{sec:unknown-vehicles}.

\begin{figure}[!htbp]
    \centering
    \begin{subfigure}[c]{0.48\textwidth}
        \centering
        \includegraphics[height=7.5cm]{figures/results/5_1_2_v11s_cm_pre.png}
        \caption{Validation confusion matrix for the baseline YOLOv11s model before dataset refinement.}
        \label{fig:val-cm-11s-pre}
    \end{subfigure}
    \hfill
    \begin{subfigure}[c]{0.48\textwidth}
        \centering
        \includegraphics[height=7.5cm]{figures/results/5_1_2_v26s_cm_pre.png}
        \caption{Validation confusion matrix for the baseline YOLOv26s model before dataset refinement}
        \label{fig:val-cm-26s-pre}
    \end{subfigure}
    \caption{Ignore-aware validation confusion matrices for the baseline YOLOv11s
             and YOLOv26s models prior to dataset refinement. No annotations were 
             modified directly from model predictions. Instead, questionable cases 
             were reviewed individually and corrected only when a labeling 
             issue was confirmed.}
    \label{fig:val-cm-pre}
\end{figure}
\FloatBarrier

After this review, the dataset was refined by reassessing flagged examples individually.
Vehicles whose labels could be determined more confidently from the imagery were 
reassigned to the appropriate class, while vehicles that remained too ambiguous 
were labeled as \verb|unknown_vehicle| and excluded from the learnable class set. 
The post-refinement validation confusion matrices for the YOLOv11s and YOLOv26s 
models are shown in Figure~\ref{fig:val-cm-post}. Relative to the pre-refinement 
results, these matrices show larger values along the main diagonal and smaller 
values in many off-diagonal cells, suggesting improved class consistency after 
dataset refinement. In particular, the number of cases in which passenger cars 
were predicted for background regions was reduced.

\begin{figure}[!htbp]
    \centering
    \begin{subfigure}[c]{0.48\textwidth}
        \centering
        \includegraphics[height=7.5cm]{figures/results/5_1_2_v11s_cm_post.png}
        \caption{Validation confusion matrix for the baseline YOLOv11s model after dataset refinement.}
        \label{fig:val-cm-11s-post}
    \end{subfigure}
    \hfill
    \begin{subfigure}[c]{0.48\textwidth}
        \centering
        \includegraphics[height=7.5cm]{figures/results/5_1_2_v26s_cm_post.png}
        \caption{Validation confusion matrix for the baseline YOLOv26s model after dataset refinement}
        \label{fig:val-cm-26s-post}
    \end{subfigure}
    \caption{Ignore-aware validation confusion matrices for the baseline YOLOv11s
             and YOLOv26s models prior to dataset refinement.}
    \label{fig:val-cm-post}
\end{figure}
\FloatBarrier

As shown in Table~\ref{tab:dataset-refinement-results}, the dataset refinement 
step led to small but consistent improvements in validation performance for both 
baseline models. YOLOv11s improved from an AP50 of 0.785 to 0.795 and from an 
mAP of 0.606 to 0.613, while YOLOv26s improved from an AP50 of 0.774 to 0.781 
and from a mAP of 0.622 to 0.627. These gains were modest, but they support 
the conclusion that manually reassessing flagged examples and preserving genuinely 
ambiguous vehicles as \verb|unknown_vehicle| improved dataset quality.

\begin{table}[!htbp]
    \centering
    \caption{Validation performance before and after dataset refinement for the 
             baseline YOLOv11s and YOLOv26s models. The best AP50 and mAP values 
            for each model are displayed in bold.}
    \label{tab:dataset-refinement-results}
    \begin{tabular}{l l c c}
        \toprule
        Base Model & Dataset Version & AP50 & mAP \\
        \midrule
        YOLOv11s & Pre-refinement  & 0.785 & 0.606 \\
                 & Post-refinement & \textbf{0.795} & \textbf{0.613} \\
        \midrule
        YOLOv26s & Pre-refinement  & 0.774 & 0.622 \\
                 & Post-refinement & \textbf{0.781} & \textbf{0.627} \\
        \bottomrule
    \end{tabular}
\end{table}
\FloatBarrier

Table~\ref{tab:val-class-counts-refinement} summarizes the validation-set class 
counts before and after dataset refinement. The number of labeled instances increased 
slightly in most supervised classes, including passenger car, passenger truck, 
truck, bus, and motorcycle, while light commercial truck decreased by one instance.

\begin{table}[!htbp]
    \centering
    \caption{Validation-set class counts before and after dataset refinement.}
    \label{tab:val-class-counts-refinement}
    \begin{tabular}{l c c}
        \toprule
        Class & Pre-Refinement & Post-Refinement \\
        \midrule
        passenger\_car            & 304  & 311  \\
        passenger\_truck          & 521  & 524  \\
        light\_commercial\_truck  & 94   & 93   \\
        truck                     & 119  & 120  \\
        bus                       & 50   & 53   \\
        motorcycle                & 4    & 6    \\
        unknown\_vehicle          & 833  & 928  \\
        \midrule
        all                       & 1925 & 2035 \\
        \bottomrule
    \end{tabular}
\end{table}
\FloatBarrier

% ------------------------------------------------------------------------------

\textbf{Hard Negative Mining}

The primary refinement strategy that followed was hard negative mining. As indicated
by the post-refinement validation confusion matrices in Figure~X, the dominant 
remaining error for both baseline models was the confusion between passenger cars 
and passenger trucks. To target this directly, the training set was scanned using
the baseline YOLOv11s model for YOLOv11s refinement and the baseline YOLOv26s 
model for YOLOv26s refinement to identify images in which a ground-truth passenger
car was predicted as a passenger truck, or a ground-truth passenger truck was 
predicted as a passenger car. In this mining step, each ground-truth passenger 
car or passenger truck was matched to its highest-overlap prediction, and an 
image was flagged when that match exceeded an IoU threshold of 0.50 but was 
assigned to the opposite class. Figure~\ref{fig:hard-negative-example} shows an 
example of a hard negative identified by this procedure, in which vehicles were 
confused between the passenger car and passenger truck classes.

\begin{figure}[!htbp]
    \centering
    \begin{subfigure}[c]{0.48\textwidth}
        \centering
        \includegraphics[height=5.5cm]{figures/results/5_1_2_hardneg_original.png}
        \caption{Original training image before hard negative visualization.}
        \label{fig:hardneg-original}
    \end{subfigure}
    \hfill
    \begin{subfigure}[c]{0.48\textwidth}
        \centering
        \includegraphics[height=5.5cm]{figures/results/5_1_2_hardneg_boxes.png}
        \caption{Same image with hard negative passenger car - passenger truck confusions identified by the mining procedure.}
        \label{fig:hardneg-boxes}
    \end{subfigure}
    \caption{Example of a hard negative identified during passenger car - passenger 
    truck mining with the YOLOv11s baseline. Subfigure (a) shows the original training image, while subfigure 
    (b) shows the same image with class-swap detections highlighted. \textcolor{blue}{Blue} 
    boxes denote passenger trucks predicted as passenger cars, and \textcolor{darkgreen}{green}
    boxes denote passenger cars predicted as passenger trucks. The number next 
    to the bounding boxes represents the prediction confidence score. These visually 
    difficult boundary cases were duplicated back into the training set during 
    retraining so that the detector would be exposed more frequently to the most 
    informative class confusions.}
    \label{fig:hard-negative-example}
\end{figure}

A sweep was performed over the mining confidence threshold and the number of difficult 
images retained, and the strongest configuration used a confidence threshold of 
0.05 with the top 100 difficult images added back into the training set. This 
targeted refinement produced the best-performing detector in the study. Compared
with the baseline YOLOv11s experiment, training a new YOLOv11s model on the 
original training set augmented with the mined hard examples improved validation
performance from AP50 = 0.804 to 0.857 and from mAP = 0.615 to 0.661. This was 
the strongest overall result obtained among the YOLO experiments.

Additional data-centric experiments were also explored through selective oversampling
of weaker classes, including \verb|light_commercial_truck|, \verb|truck|, and \verb|motorcycle|. 
However, these broader class-specific duplication strategies did not improve 
upon the best hard-negative-mined configuration. A local grid search was also 
performed around the baseline hyperparameter settings to determine whether 
further gains could be obtained through optimization alone, but none of the 
tested configurations improved upon the best hard-negative-mined YOLOv11s model 
in either AP50 or mAP. Comparable hard-negative experiments were also conducted 
using YOLOv26s, with additional variation in the mining confidence threshold 
and the number of selected images. Although these runs produced small improvements,
none exceeded the best refined YOLOv11s result. Further refinement runs were 
also carried out by initializing training from previously trained baseline and 
refined YOLOv11s and YOLOv26s checkpoints rather than from the standard pretrained 
Ultralytics weights. In these cases, training generally plateaued at lower AP50 
and mAP values, even when hyperparameters were varied, indicating that continued
fine-tuning from existing checkpoints was less effective than retraining a new 
model on the augmented dataset. Selected strong-performing YOLO results across 
the YOLOv11s and YOLOv26s backbones, including baseline, mined, oversampled, 
hyperparameter-search, and continued fine-tuning configurations, are summarized 
in Table~\ref{tab:yolo-refinement-summary}.

\begingroup
\small
\renewcommand{\arraystretch}{1.15}
\arrayrulecolor{black!25}
\setlength{\tabcolsep}{4pt}

\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{1.38cm} >{\centering\arraybackslash}p{1.85cm} >{\raggedright\arraybackslash}p{3.05cm} >{\centering\arraybackslash}p{1.0cm} >{\centering\arraybackslash}p{1.15cm} >{\raggedright\arraybackslash}X}
\caption{Summary of the principal YOLO refinement experiments conducted under the MOVES-based taxonomy. AP50 and mAP are reported at the best validation checkpoint for each run. Extra images refers to the additional images added to the training set.}
\label{tab:yolo-refinement-summary} \\
\toprule
Backbone & Extra Images & Hyperparameters & AP50 & mAP & \multicolumn{1}{c}{Notes} \\
\midrule
\endfirsthead

\toprule
Backbone & Extra Images & Hyperparameters & AP50 & mAP & \multicolumn{1}{c}{Notes} \\
\midrule
\endhead

\midrule
\multicolumn{6}{r}{Continued on next page} \\
\midrule
\endfoot

\bottomrule
\endlastfoot

YOLOv11s & 0 & Default YOLO & 0.804 & 0.615 & Baseline YOLOv11s model. \\
\hline
YOLOv11s & 100 & Default YOLO & 0.857 & 0.661 & Added 100 mined \texttt{passenger\_car} $\leftrightarrow$ \texttt{passenger\_truck} hard negatives identified using the baseline YOLOv11s model. \\
\hline
YOLOv11s & 400 & Default YOLO & 0.776 & 0.597 & Added mined \texttt{passenger\_car} $\leftrightarrow$ \texttt{passenger\_truck} hard negatives identified using the baseline YOLOv11s model. Additionally oversampled 300 images containing \texttt{light\_commercial\_truck}, \texttt{truck}, and \texttt{motorcycle} instances. \\
\hline
YOLOv11s & 1000 &
\begin{tabular}[t]{@{}l@{}}
lr0 = 0.002; \\
lrf = 0.01; \\
close\_mosaic = 10; \\
mixup = 0.0; \\
copy\_paste = 0.0
\end{tabular}
& 0.831 & 0.648 & Fine-tuned from the best-performing YOLOv11s model after adding mined \texttt{passenger\_car} $\leftrightarrow$ \texttt{passenger\_truck} hard negatives, 830 oversampled \texttt{light\_commercial\_truck} and \texttt{truck} examples, and 70 additional images containing \texttt{motorcycle} instances. \\
\hline
YOLOv11s & 100 & Default YOLO with horizontal flipping disabled & 0.856 & 0.657 & Best non-default local hyperparameter-search run. \\
\hline
YOLOv26s & 0 & Default YOLO & 0.802 & 0.632 & Baseline YOLOv26s model. \\
\hline
YOLOv26s & 100 & Default YOLO & 0.818 & 0.637 & Added 100 mined \texttt{passenger\_car} $\leftrightarrow$ \texttt{passenger\_truck} hard negatives identified using the baseline YOLOv26s model. \\
\hline
YOLOv26s & 100 &
\begin{tabular}[t]{@{}l@{}}
lr0 = 0.001; \\
cosine LR; \\
close\_mosaic = 10; \\
mixup disabled; \\
freeze = 10
\end{tabular}
& 0.843 & 0.640 & Fine-tuned from the baseline YOLOv26s model after adding 100 mined \texttt{passenger\_car} $\leftrightarrow$ \texttt{passenger\_truck} hard negatives. \\
\end{tabularx}

\arrayrulecolor{black}
\endgroup

Overall, the data-centric refinement experiments showed that the largest performance 
gains were obtained from adding mined hard examples targeting the 
\verb|passenger_car|–\verb|passenger_truck| boundary, whereas broader
class-specific oversampling and additional hyperparameter changes did not yield 
further improvement.

% ------------------------------------------------------------------------------

\subsubsection{Final YOLO Model Selection and Test-Set Evaluation}

Based on the baseline comparisons and subsequent data-centric refinement experiments, 
the final detector selected for this study was YOLOv11s, saved as checkpoint 
\verb|vehicle_6c_v11s11|. For readability, this checkpoint is referred to 
throughout the remainder of the thesis as the final YOLOv11s model. Among all 
YOLO experiments, this configuration achieved the strongest validation performance,
with an AP50 of 0.857 and a mAP of 0.661.

Although additional refinement runs were explored after this stage, none improved 
upon the validation performance of the final YOLOv11s model. It was therefore 
selected for all remaining evaluations and was assessed once on the held-out test
set, which had been reserved exclusively for final evaluation. The test set was 
evaluated using the same ignore-aware procedure used for validation, as outlined 
in Section~\ref{sec:unknown-vehicles}. The resulting test-set performance is 
summarized by class in Table~\ref{tab:yolo-final-test-results}.

\begin{table}[!htbp]
    \centering
    \caption{Held-out test-set performance of the final YOLOv11s model, reported by class.}
    \label{tab:yolo-final-test-results}
    \begin{tabular}{l c c c c}
        \toprule
        Class & Images & Instances & AP50 & mAPj \\
        \midrule
        passenger\_car            & 114 & 363  & 0.779 & 0.620 \\
        passenger\_truck          & 117 & 643  & 0.868 & 0.705 \\
        light\_commercial\_truck  & 62  & 110  & 0.867 & 0.703 \\
        truck                     & 42  & 65   & 0.926 & 0.701 \\
        bus                       & 44  & 67   & 0.895 & 0.703 \\
        motorcycle                & 5   & 5    & 0.298 & 0.209 \\
        \midrule
        all                       & 120 & 1253 & 0.772 & 0.607 \\
        \bottomrule
    \end{tabular}
\end{table}
\FloatBarrier

With the final YOLOv11s model selected and evaluated on the held-out test set, 
the next section compares these YOLO results with those obtained from alternative 
detection architectures.

% ==============================================================================

\subsection{Comparison with Alternative Detection Architectures}

\subsubsection{Faster R-CNN}

Like the YOLO experiments, Faster R-CNN experiments were conducted under the 
MOVES-based taxonomy. For implementation, torchvision Faster R-CNN models with 
ResNet-50 feature pyramid backbones were used, including both the standard 
\verb|fasterrcnn_resnet50_fpn| model and the newer \verb|fasterrcnn_resnet50_fpn_v2|
variant provided in torchvision. The \verb|fasterrcnn_resnet50_fpn_v2| model is 
described in the torchvision documentation as an improved Faster R-CNN model with 
a ResNet-50-FPN backbone \cite{torchvision2016}.

For the Faster R-CNN experiments, the detector head was adapted to the MOVES-based
taxonomy by replacing the default box predictor with a new predictor for six 
foreground classes plus background. The training pipeline used the same train/validation
split as the other detectors. A small-object-oriented anchor generator was used 
with anchor sizes of 8, 16, 32, 64, and 128 pixels across the feature pyramid, 
and like the YOLO experiments, images were resized to $768 \times 1280$. Training used 
AdamW with a learning rate of $2\times10^{-4}$, weight decay of $10^{-4}$, and 
a cosine annealing learning-rate schedule over 50 epochs. Light augmentation was 
applied during training, consisting of random horizontal flipping and modest 
color jitter in the best runs. Batch size was set to 4 for training and 2 for 
validation. As in the YOLO and transformer experiments, \verb|unknown_vehicle| 
instances were masked during training and treated as ignore regions during evaluation, 
following the same ignore-aware procedure outlined in Section~\ref{sec:unknown-vehicles}.

A light targeted grid search was conducted for the Faster R-CNN experiments to 
evaluate a small number of influential training choices without the cost of a 
full exhaustive sweep. The search varied four factors: backbone variant 
(\verb|fasterrcnn_resnet50_fpn_v2| versus \verb|fasterrcnn_resnet50_fpn|), \
learning rate ($1\times10^{-4}$ and $2\times10^{-4}$), augmentation strategy 
(random horizontal flipping only versus horizontal flipping with modest color 
jitter), and the number of trainable backbone layers (3 versus 5). Weight decay
was held fixed at $1\times10^{-4}$, and each run was trained for 50 epochs under
the same general pipeline. Table~\ref{tab:faster-rcnn-results} summarizes the 
strongest Faster R-CNN results discussed in this section, while Appendix~\ref{app:A} 
reports the full set of grid-search results.

\begin{table}[!htbp]
    \centering
    \caption{Validation performance of the Faster R-CNN experiments under the MOVES-based taxonomy.}
    \label{tab:faster-rcnn-results}
    \begin{tabular}{l c c c c c}
        \toprule
        Backbone & Learning Rate & Augmentation & \shortstack{Trainable\\Backbone\\Layers} & AP50 & \shortstack{mAP} \\
        \midrule
        ResNet-50-FPN    & 1e-4 & Flip + Color Jitter & 5 & 0.658 & 0.440 \\
        ResNet-50-FPN-v2 & 2e-4 & Flip + Color Jitter & 5 & 0.706 & 0.509 \\
        \bottomrule
    \end{tabular}
\end{table}
\FloatBarrier

Among the tested configurations, the best overall Faster R-CNN result was obtained
with the \verb|fasterrcnn_resnet50_fpn_v2| backbone using a learning rate of $2\times10^{-4}$,
flip-plus-color-jitter augmentation, and 5 trainable backbone layers. On the 
validation set, this model achieved an AP50 of 0.706 and a mAP of 0.509. Other 
strong \verb|fasterrcnn_resnet50_fpn_v2| runs achieved AP50 and mAP values of 0.693/0.686 
and 0.500/0.497, indicating that the v2 backbone was consistently the strongest 
Faster R-CNN variant across the tested settings. By comparison, the best standard 
\verb|fasterrcnn_resnet50_fpn| configuration achieved an AP50 of 0.658 and a mAP 
of 0.440. 

Although the \verb|fasterrcnn_resnet50_fpn_v2| experiments produced the strongest 
Faster R-CNN results in this study, their validation performance remained below that 
of the YOLO baselines reported in Table~\ref{tab:yolo-baseline-results}. In particular,
the best Faster R-CNN configuration achieved an AP50 of 0.706 and a mAP of 0.509, 
whereas the strongest baseline YOLO results were already higher before additional 
refinement. Since the Faster R-CNN baselines did not approach the performance of the 
leading YOLO configurations, further iteration on the Faster R-CNN models was not
pursued.

% ------------------------------------------------------------------------------

\subsubsection{Detection Transformer (DETR)}

Three transformer-based detectors were evaluated under the MOVES-based taxonomy: 
SenseTime Deformable DETR, Peking University RT-DETR, and Peking University RT-DETRv2. 
These models were selected to provide a comparison across recent DETR-style detector 
designs within a common implementation framework. Deformable DETR served as an 
established multi-scale transformer baseline, while RT-DETR and RT-DETRv2 represented 
more efficiency-oriented DETR variants intended to improve the speed-accuracy tradeoff 
\cite{detr2020,deformabledetr2020,rtdetr2024,rtdetrv22024}.

For these experiments, the detector backbone was kept frozen for the first 10 epochs 
and then unfrozen for the remainder of training, with the full model trained for 50 
epochs using AdamW and a weight decay of $10^{-4}$. A cosine learning-rate schedule
was used, and learning rates were selected through iterative tuning rather than 
exhaustive search, since a single training run required several hours. In practice, 
parameter settings were chosen by monitoring whether validation performance remained 
stable and continued improving over training, while avoiding configurations that caused
the detector to stall or degrade early. The best learning rates for Deformable DETR 
and RT-DETR are summarized in Table~X.

Training augmentation was ultimately kept light, consisting primarily of horizontal
flipping and modest brightness/contrast perturbations. This decision was motivated 
by earlier experiments using a heavier Albumentations pipeline, which suggested
that transformer-based detectors were sensitive to stronger image transformations
in this low-resolution traffic-camera setting. In this section, heavier Albumentations 
pipeline refers to the augmentation setup used in the first Deformable DETR 
experiment, and lighter Albumentations pipeline refers to the simplified setup used
in the subsequent experiments. The exact transform definitions are listed in Appendix~\ref{app:B}.
As in the YOLO and R-CNN experiments, \verb|unknown_vehicle| instances were masked 
during training, and treated as ignore regions during evaluation.

\begin{table}[!htbp]
    \centering
    \caption{Validation performance of the transformer-based detector experiments under the final 6-class formulation. All runs were trained for 50 epochs with 10 warmup epochs.}
    \label{tab:detr-results}
    \small
    \renewcommand{\arraystretch}{1.15}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{l c c c c c c}
        \toprule
        Backbone & \shortstack{Head LR\\(Frozen)} & \shortstack{Backbone LR\\(Unfrozen)} & \shortstack{Head LR\\(Unfrozen)} & Augmentations & AP50 & mAP \\
        \midrule
        SenseTime/deformable-detr & $1\times10^{-4}$ & $1\times10^{-5}$ & $7\times10^{-5}$ & Heavy & 0.428 & 0.318 \\
        SenseTime/deformable-detr & $1\times10^{-4}$ & $1\times10^{-5}$ & $7\times10^{-5}$ & Light & 0.564 & 0.421 \\
        PekingU/rtdetr\_r50vd     & $1\times10^{-5}$ & $5\times10^{-6}$ & $7\times10^{-6}$ & Light & 0.596 & 0.437 \\
        PekingU/rtdetr\_v2\_r50vd & $1\times10^{-5}$ & $5\times10^{-6}$ & $7\times10^{-6}$ & Light & 0.688 & 0.516 \\
        \bottomrule
    \end{tabular}%
    }
\end{table}
\FloatBarrier

The transformer experiments showed a clear progression across the tested DETR 
variants. For SenseTime Deformable DETR, an initial 50-epoch run achieved an AP50 
of 0.428 and a mAP of 0.318, while a second run with less aggressive image 
transforms improved performance to 0.564 AP50 and 0.421 mAP. Additional 
hyperparameter and augmentation adjustments were also tested, but none exceeded 
this result. Under the same 50-epoch training schedule, RT-DETR achieved an AP50 
of 0.596 and a mAP of 0.437, and RT-DETRv2 produced the strongest transformer-based
result in this study, reaching an AP50 of 0.688 and a mAP of 0.516.

Although RT-DETRv2 was the strongest transformer-based model evaluated, its 
performance still remained below the baseline YOLO results reported in 
Table~\ref{tab:yolo-baseline-results}. Accordingly, the DETR models were not 
pursued further as candidates for the final detector.

% ==============================================================================

\subsection{Model Robustness and Cross-Camera Generalization}

After selecting the final detector, additional experiments were conducted to evaluate 
its robustness and generalization beyond the original validation split. These 
experiments focused on two questions. First, the sensitivity of the detector to
confidence-threshold and non-maximum-suppression settings was examined to determine 
how stable performance remained under different inference configurations. Second, 
cross-camera generalization was assessed using a manually annotated benchmark of 
75 images collected from three previously unseen inference cameras. These images 
were sampled between March 12 and March 20, 2026, with 25 images per camera selected 
at varying times of day in order to capture a range of traffic and viewing conditions.

\subsubsection{Confidence Threshold and NMS Sensitivity Analysis}

To evaluate how dependent the final detector was on inference-time settings, a 
threshold and non-maximum suppression (NMS) sensitivity analysis was performed 
using the final YOLOv11s model. In this context, the confidence threshold determines
the minimum prediction confidence required for a detection to be kept, while 
the NMS IoU threshold controls how aggressively overlapping detections are suppressed 
during post-processing \cite{ultralytics_validation}. 
Lower confidence thresholds retain more candidate detections,
typically increasing recall at the expense of additional false positives, whereas 
higher thresholds are more selective. Similarly, lower NMS IoU thresholds suppress
overlapping boxes more aggressively, while higher values allow more overlapping 
detections to remain.

This tradeoff is illustrated qualitatively in Figure~\ref{fig:nms-threshold-example}.
Each panel shows the same traffic scene under a different inference configuration, 
with red boxes indicating detections and adjacent numbers indicating confidence scores.
In Figure~\ref{fig:nms-threshold-example-a}, a very low confidence threshold 
(\verb|conf| = 0.001) and a relatively permissive NMS IoU threshold 
(\verb|det_iou| = 0.70) retain many detections, including several overlapping boxes 
around the same vehicles. In Figure~\ref{fig:nms-threshold-example-b}, the confidence
threshold remains at 0.001 but the NMS IoU threshold is reduced to 0.40, producing 
fewer overlapping detections. In Figure~\ref{fig:nms-threshold-example-c}, the NMS
IoU threshold remains at 0.40 while the confidence threshold is increased to 0.10, 
removing many weaker detections and leaving a sparser set of predictions. Together,
these examples show how stricter inference settings can reduce duplicate detections 
and visual clutter, but may also suppress vehicles that would otherwise be counted.

\begin{figure}[!htbp]
    \centering

    \begin{subfigure}{0.92\textwidth}
        \centering
        \includegraphics[height=5.5cm]{figures/results/5_3_1_nms_th_top.png}
        \caption{Same scene evaluated with \protect\texttt{conf} = 0.001 and \protect\texttt{det\_iou} = 0.70.}
        \label{fig:nms-threshold-example-a}
    \end{subfigure}

    \vspace{0.75em}

    \begin{subfigure}{0.92\textwidth}
        \centering
        \includegraphics[height=5.5cm]{figures/results/5_3_1_nms_th_mid.png}
        \caption{Same scene evaluated with \protect\texttt{conf} = 0.001 and \protect\texttt{det\_iou} = 0.40.}
        \label{fig:nms-threshold-example-b}
    \end{subfigure}

    \vspace{0.75em}

    \begin{subfigure}{0.92\textwidth}
        \centering
        \includegraphics[height=5.5cm]{figures/results/5_3_1_nms_th_bot.png}
        \caption{Same scene evaluated with \protect\texttt{conf} = 0.10 and \protect\texttt{det\_iou} = 0.40.}
        \label{fig:nms-threshold-example-c}
    \end{subfigure}

    \caption{Qualitative example of confidence-threshold and NMS sensitivity for the final YOLOv11s model. Each panel shows the same traffic scene under a different inference configuration, with red boxes indicating retained detections and adjacent numbers indicating confidence scores. Lowering the NMS IoU threshold reduces overlapping detections, while increasing the confidence threshold further suppresses weaker predictions.}
    \label{fig:nms-threshold-example}
\end{figure}

To examine the stability of the detector across a reasonable operating range, the 
confidence threshold was swept over
\[
\{0.001,\ 0.005,\ 0.01,\ 0.05,\ 0.10,\ 0.25,\ 0.35,\ 0.50,\ 0.75\}
\]
and the NMS IoU threshold was swept over
\[
\{0.40,\ 0.50,\ 0.60,\ 0.70\}.
\]

For each combination, predictions were evaluated on the validation set using the
same ignore-aware evaluation procedure described in Section~\ref{sec:unknown-vehicles},
in which detections overlapping \verb|unknown_vehicle| regions above the specified
ignore IoU threshold were removed before scoring. This produced a grid of 36 inference
configurations, allowing the sensitivity of both AP50 and mAP to be assessed without 
using the held-out test set for tuning. Figure~\ref{fig:conf-nms-heatmaps} presents 
heatmaps of validation AP50 and mAP across the evaluated confidence-threshold and 
NMS IoU grid for the final YOLOv11s model.

\begin{figure}[!htbp]
    \centering
    \begin{subfigure}[c]{0.48\textwidth}
        \centering
        \includegraphics[height=5.5cm]{figures/results/5_3_1_ap50_conf_nms.png}
        \caption{Validation AP50 across confidence-threshold and NMS IoU settings for the final YOLOv11s model.}
        \label{fig:conf-nms-ap50}
    \end{subfigure}
    \hfill
    \begin{subfigure}[c]{0.48\textwidth}
        \centering
        \includegraphics[height=5.5cm]{figures/results/5_3_1_map_conf_nms.png}
        \caption{Validation mAP across confidence-threshold and NMS IoU settings for the final YOLOv11s model.}
        \label{fig:conf-nms-map}
    \end{subfigure}
    \caption{Heatmaps showing the sensitivity of validation performance to inference-time confidence and NMS IoU settings for the final YOLOv11s model. Subfigure (a) shows AP50 and subfigure (b) shows mAP over the evaluated confidence-threshold and NMS IoU grid.}
    \label{fig:conf-nms-heatmaps}
\end{figure}
\FloatBarrier

As shown in Figure~\ref{fig:conf-nms-heatmaps}, both AP50 and mAP were relatively 
stable across the lower confidence-threshold range, with the strongest values 
occurring near \verb|conf| = 0.001 and higher \verb|det_iou| settings. Performance
then declined gradually as the confidence threshold increased to 0.25, followed 
by a more noticeable drop beginning at \verb|conf| = 0.35 and a sharper decrease
at \verb|conf| = 0.75. By contrast, variation across the NMS IoU threshold was 
comparatively small throughout the sweep, with changes in \verb|det_iou| producing 
only modest differences relative to the effect of the confidence threshold. These 
results indicate that detector performance was much more sensitive to aggressive 
confidence filtering than to the specific NMS IoU setting within the tested range.

% ------------------------------------------------------------------------------

\subsubsection{Cross-Camera Generalization}
\label{sec:camera-generalization}

To assess how well the final YOLO detector can predict vehicle classes beyond the 
original train-validation-test split, a separate manually annotated benchmark was 
constructed using images from three traffic cameras unseen by the model during 
training. These cameras were located at Atlantic Avenue at Boerum Place, Broadway
at Houston, and Pelham Parkway Westbound at Boston Road. These locations were 
selected not only to provide unseen camera views for detector evaluation, but also
because they were later used in the traffic-count comparison in Section~\ref{sec:atr-analysis}.

The benchmark contained 75 images in total, with 25 images sampled from each camera. 
These images were drawn from March 12 to March 20, 2026, and were selected across 
different times of day in order to capture a broader range of traffic density, 
lighting, and scene conditions. The resulting benchmark was intended to better 
approximate deployment conditions than the original validation set, since none of
these camera views were included during training. The full class distribution of 
the benchmark is summarized in Table~\ref{tab:cross-camera-benchmark-class-counts}.

\begin{table}[!htbp]
    \centering
    \caption{Class distribution of the 75-image cross-camera benchmark.}
    \label{tab:cross-camera-benchmark-class-counts}
    \begin{tabular}{l c}
        \toprule
        Class & Instances \\
        \midrule
        passenger\_car            & 107 \\
        passenger\_truck          & 209 \\
        light\_commercial\_truck  & 25  \\
        truck                     & 13  \\
        bus                       & 8   \\
        motorcycle                & 2   \\
        unknown\_vehicle          & 474 \\
        \midrule
        all                       & 838 \\
        \bottomrule
    \end{tabular}
\end{table}
\FloatBarrier

The cross-camera benchmark was evaluated using the same ignore-aware procedure as 
described in Section~\ref{sec:unknown-vehicles}. Table~\ref{tab:cross-camera-results}
summarizes the per-class results on this 75-image benchmark. Overall, the final 
YOLOv11s model achieved an AP50 of 0.658 and a mAP of 0.488 across 364 annotated 
vehicle instances.

\begin{table}[!htbp]
    \centering
    \caption{Per-class detection performance of the final YOLOv11s model on the 75-image cross-camera benchmark.}
    \label{tab:cross-camera-results}
    \begin{tabular}{l c c}
        \toprule
        Class & AP50 & mAP \\
        \midrule
        passenger\_car            & 0.626 & 0.487 \\
        passenger\_truck          & 0.761 & 0.592 \\
        light\_commercial\_truck  & 0.685 & 0.488 \\
        truck                     & 0.735 & 0.514 \\
        bus                       & 0.635 & 0.490 \\
        motorcycle                & 0.505 & 0.353 \\
        \midrule
        all                       & 0.658 & 0.488 \\
        \bottomrule
    \end{tabular}
\end{table}
\FloatBarrier


% ==============================================================================

\subsection{Comparison with ATR Traffic Counts}
\label{sec:atr-analysis}

This experiment was conducted to assess how closely hourly vehicle counts produced
by the final YOLOv11s model aligned with New York City Automated Traffic Recorder 
(ATR) counts \cite{NYC_open_data_atr} at three unseen camera locations: Atlantic 
Avenue at Boerum Place, Broadway at Houston, and Pelham Parkway Westbound at Boston 
Road. These were the same three locations used in the cross-camera benchmark in 
Section~\ref{sec:camera-generalization}, allowing the detector's performance on
unseen views to be examined further through a traffic-count comparison.

\begin{figure}[!htbp]
    \centering
    \begin{subfigure}[t]{0.32\textwidth}
        \centering
        \includegraphics[height=3.5cm]{figures/results/5_4_new_broadway_ex.jpg}
        \caption{Broadway at Houston}
        \label{fig:new-broad-ex}
    \end{subfigure}\hfill
    \begin{subfigure}[t]{0.32\textwidth}
        \centering
        \includegraphics[height=3.5cm]{figures/results/5_4_new_pelham_ex.jpg}
        \caption{Pelham Parkway Westbound at Boston Road}
        \label{fig:new-pel-ex}
    \end{subfigure}\hfill
    \begin{subfigure}[t]{0.32\textwidth}
        \centering
        \includegraphics[height=3.5cm]{figures/results/5_4_new_atl_ex.jpg}
        \caption{Atlantic Avenue at Boerum Place}
        \label{fig:new-atl-ex}
    \end{subfigure}
    \caption{New camera imagery used for inference.}
    \label{fig:new-cameras}
\end{figure}
\FloatBarrier

The three camera locations were selected because they were positioned on urban 
streets rather than major highways, which were not the primary focus of this study. 
Because March 2026 ATR data were not available at the time the camera imagery 
was collected, the comparison instead used March 2025 ATR records from locations
where an active traffic camera was available at the same street intersection. 
March 2026 camera imagery was then matched to March 2025 ATR dates with broadly 
similar weather conditions \cite{zippenfenig_open-meteocom_2024}.

Images were collected from March 13, 2026 at 12:01 AM through March 20, 2026 at 
12:00 PM. An image was sampled from each camera every two minutes, consistent with
the sampling rate used during dataset creation, to reduce the risk of the same 
scene being counted more than once, since the camera feeds updated slowly and 
explicit image-to-image vehicle matching was not implemented.
See Table X. for information regarding the
dates in which the collected images are compared against the true ATR counts. 
These comparison dates were chosen as they were when the 2025 ATR data was collected 
for the specified street intersections.

\begin{table}[!htbp]
    \centering
    \caption{Camera locations, comparison dates, and total number of collected images used in the ATR traffic-count analysis.}
    \label{tab:atr-image-collection}
    \begin{tabular}{p{7cm} c c c}
        \toprule
        \centering Camera Intersection &
        \centering Borough &
        \centering\shortstack{Vehicle Count\\Comparison Dates} &
        \centering Total Images \tabularnewline
        \midrule
        Broadway @ Houston           & Manhattan & 3/14 -- 3/17 & 2,774 \\
        Pelham Pkwy W/B @ Boston Rd  & Bronx     & 3/13 -- 3/17 & 3,488 \\
        Atlantic Ave @ Boerum Place  & Brooklyn  & 3/18 -- 3/19 & 1,440 \\
        \bottomrule
    \end{tabular}
\end{table}
\FloatBarrier

After the images were collected, inference was run over all images using the final 
YOLOv11s model. Based on the validation heatmaps in Figure~\ref{fig:conf-nms-heatmaps}, 
performance remained relatively stable at lower confidence thresholds but declined more 
noticeably beyond 0.25, so a confidence threshold of 0.25 and an NMS IoU threshold of 
0.70 were used for inference. The resulting detections were aggregated into hourly 
vehicle counts and matched to ATR hourly traffic volumes by camera-specific road 
segment and hour of day. Because the ATR data were reported in 15-minute intervals, 
those records were first summed to hourly totals before comparison. See Table~\ref{tab:atr-count-comparison} 
for a per-day summary at each camera location, comparing the YOLO and ATR detections.

\begin{table}[!htbp]
    \centering
    \caption{Per-day comparison of YOLO-derived and ATR vehicle counts across the selected camera locations, including hourly-profile Pearson correlation, mean absolute error, and mean absolute percentage error.}
    \label{tab:atr-count-comparison}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{l c c c c c c}
        \toprule
        \shortstack{Camera\\Intersection} &
        Date &
        \shortstack{YOLO Count\\(2026)} &
        \shortstack{ATR Count\\(2025)} &
        \shortstack{Pearson\\Correlation} &
        MAE &
        \shortstack{MAPE\\(\%)} \\
        \midrule
        Broadway @ Houston                  & 3/14/2026 & 4,752 & 16,252 & 0.608 & 479.17 & 68.29 \\
        Broadway @ Houston                  & 3/15/2026 & 4,709 & 15,626 & 0.597 & 454.88 & 69.08 \\
        Broadway @ Houston                  & 3/16/2026 & 3,431 & 12,382 & 0.234 & 393.17 & 67.22 \\
        Broadway @ Houston                  & 3/17/2026 & 3,910 & 13,067 & 0.753 & 398.13 & 68.31 \\
        Pelham Pkwy W/B @ Boston Rd         & 3/13/2026 & 5,793 & 20,043 & 0.892 & 593.75 & 70.66 \\
        Pelham Pkwy W/B @ Boston Rd         & 3/14/2026 & 4,947 & 20,423 & 0.866 & 644.83 & 70.78 \\
        Pelham Pkwy W/B @ Boston Rd         & 3/15/2026 & 4,181 & 17,742 & 0.897 & 565.04 & 76.15 \\
        Pelham Pkwy W/B @ Boston Rd         & 3/16/2026 & 4,107 & 13,791 & 0.780 & 421.04 & 71.35 \\
        Pelham Pkwy W/B @ Boston Rd         & 3/17/2026 & 4,376 & 18,096 & 0.906 & 596.52 & 78.47 \\
        Atlantic Ave @ Boerum Place         & 3/18/2026 & 7,559 & 8,725  & 0.844 & 82.67  & 22.96 \\
        Atlantic Ave @ Boerum Place         & 3/19/2026 & 7,060 & 9,188  & 0.803 & 104.17 & 27.28 \\
        \bottomrule
    \end{tabular}%
    }
\end{table}
\FloatBarrier

As shown in Table~X, agreement with ATR counts varied substantially across locations. 
The closest match between counts was observed at Atlantic Avenue at Boerum Place,
where detector totals of 7,060 to 7,559 were compared with ATR totals of 8,725 to 
9,188, with mean absolute percentage error values of 22.96\% and 27.28\%. Broadway
at Houston showed larger discrepancies, with detector totals of 3,431 to 4,752 
versus ATR totals of 12,382 to 16,252 and mean absolute percentage error values 
ranging from 67.22\% to 69.08\%. Pelham Parkway Westbound at Boston Road exhibited 
the largest undercount overall, with detector totals of 4,107 to 5,793 compared
with ATR totals of 13,791 to 20,423 and mean absolute percentage error values 
ranging from 70.66\% to 78.47\%.

In addition to total-count error, Pearson correlation was computed for each camera-day 
pair using normalized hourly YOLO and ATR counts, where each hour was normalized 
relative to the source’s total daily vehicle count. This was done to compare how 
closely the two sources followed the same hourly traffic pattern, independent of 
differences in overall count size. Under this measure, Atlantic Avenue at Boerum 
Place showed consistently strong hourly agreement, with correlation values of 0.844
and 0.803. Broadway at Houston showed weaker and less consistent agreement, with 
values ranging from 0.234 to 0.753. Pelham Parkway Westbound at Boston Road generally 
showed strong hourly-pattern agreement despite larger differences in total volume, 
with correlations ranging from 0.780 to 0.906. See Figure X. for hourly count 
comparisons for selected camera-day pairs, showing both the raw hourly counts and 
the normalized horly counts for the YOLO and ATR detections.

\begin{figure}[!htbp]
    \centering

    % Row 1: Pelham
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/results/5_4_pel_hourly.png}
        % \includegraphics[height=3.5cm]{figures/results/5_4_new_broadway_ex.jpg}
        \caption{Pelham Parkway Westbound at Boston Road. Raw hourly counts.}
        \label{fig:atr-pelham-raw}
    \end{subfigure}\hfill
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/results/5_4_pel_hourly_norm.png}
        \caption{Pelham Parkway Westbound at Boston Road. Normalized hourly counts.}
        \label{fig:atr-pelham-norm}
    \end{subfigure}

    \vspace{0.5em}

    % Row 2: Atlantic
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/results/5_4_atl_hourly.png}
        \caption{Atlantic Avenue at Boerum Place. Raw hourly counts.}
        \label{fig:atr-atlantic-raw}
    \end{subfigure}\hfill
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/results/5_4_atl_hourly_norm.png}
        \caption{Atlantic Avenue at Boerum Place. Normalized hourly counts.}
        \label{fig:atr-atlantic-norm}
    \end{subfigure}

    \vspace{0.5em}

    % Row 3: Broadway
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/results/5_4_broad_hourly.png}
        \caption{Broadway at Houston. Raw hourly counts.}
        \label{fig:atr-broadway-raw}
    \end{subfigure}\hfill
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/results/5_4_broad_hourly_norm.png}
        \caption{Broadway at Houston. Normalized hourly counts.}
        \label{fig:atr-broadway-norm}
    \end{subfigure}

    \caption{Hourly detector-derived and ATR traffic-count comparisons for the 
             selected camera-day pairs. The plots compare YOLO-derived counts from 
             March 2026 against matched ATR counts from March 2025. Subfigures 
             (a) and (b) show Pelham Parkway Westbound at Boston Road, with a 
             YOLO-derived daily total of 4,376 and an ATR total of 18,096, and a
             Pearson correlation of 0.906. Subfigures (c) and (d) show Atlantic 
             Avenue at Boerum Place, with a YOLO-derived daily total of 7,559 and
             an ATR total of 8,725, and a Pearson correlation of 0.844. Subfigures
             (e) and (f) show Broadway at Houston, with a YOLO-derived daily total
             of 3,431 and an ATR total of 12,382, and a Pearson correlation of 0.234.
             The left column shows raw hourly counts, while the right column shows 
             normalized hourly profiles, with each hour expressed as a fraction of 
             that source's daily total.}
    \label{fig:atr-hourly-comparisons}
\end{figure}

Overall, these results showed that the final YOLOv11s model reproduced hourly 
traffic patterns more consistently at some unseen camera locations than others, 
while agreement with ATR count magnitude remained strongly location-dependent.

\FloatBarrier
